/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {
                hostname:"wbhauwsl65dfg05n.public.blob.vercel-storage.com"
            }
        ]
    }
};

export default nextConfig;
